#include <iostream>
#include<list>
using namespace std;
int main()
{
    list<string> l;
    //Add a data
    l.push_back("abc");
    l.push_back("def");
    l.push_front("ijk");
    //delete a data
    l.pop_back();
    l.pop_front();
    //print all the data
    for(string i: l)
        cout<<i;
    //print front data
    cout<<l.front();
    //print last data
    cout<<l.back();
return 0;
}
//github.com/Anand-29
