#include <iostream>
#include<queue>
using namespace std;
int main()
{
    queue<string> q;
    //Add a data
    q.push("abc");
    q.push("def");
    //deletes front  data
    q.pop();
    //access data 
    cout<<q.front();
    //check if queue is empty or not
    if(q.empty())
        cout<<"empty stack";
    else
        cout<<"not empty stack";
    return 0;
}
//github.com/Anand-29
